const Info = () => {
    return (
        <div>
            <b>Note:</b> Utility dash is a work-in-progress app. Notes can only be created/removed on client side as there is no working backend yet. <br></br>Notes disappear on reload.
        </div>
    )
}

export default Info
