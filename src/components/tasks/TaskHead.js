import Button from './Button'

const TaskHead = ({}) => {
    return (
        <header className="tasksHead">
            Tasks
            <hr></hr>
        </header>
    )
}

export default TaskHead
